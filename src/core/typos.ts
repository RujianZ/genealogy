/**
 * 2016 年排版时打错的字。
 *
 * ★ **这不是异体字。** 繁简异体（variants.ts）是同一个字的两种正当写法；
 *   这里是**排版录入时打错的字**，两回事，所以分开放。
 *
 * ★ 每一条都必须有依据，而且依据要能自己去查：
 *   查法是直接读 source/ 底下那几个 .doc——2016 年那一届是用 Word 排的
 *   （合一/合二/合三 三册世系正文是打字打进去的，卷首那本才是扫描图片）。
 *       python tools/readdoc.py "source/合二（5、6、7）.doc" 粱四
 *
 * ★ 和 variants 一样：**只用于比对，绝不用于显示。**
 *   界面上永远显示谱上原样的那个字。谱写「粱四」就显示「粱四」。
 */

export const TYPOS: Record<string, string> = {
  // 泽均那一条的生子名单写作「梁一 梁二 梁三 粱四 梁五」——连号的五个儿子，
  // 第四个的「粱」（高粱的粱）显然是「梁」打错。第 22 世梁四（册3·卷七·第286页）
  // 自己那条写「泽均公四子」，两边本该对上，就卡在这一个字上，
  // 於是他和往下 21 口人的父亲一直判不出来。
  // 依据：「粱」在三册世系正文里**总共只出现这一次**（合一 0 次、合三 0 次、合二 1 次），
  //       而且就在这份连号名单里。
  '粱': '梁',
};

/** 每条的依据，界面上要解释「为什么这两个字算同一个」时用。 */
export const TYPO_SOURCE: Record<string, string> = {
  '粱': '2016 年排版误字：「梁一梁二梁三粱四梁五」连号名单里的第四个；'
      + '「粱」在三册世系正文中仅此一见（见 source/合二（5、6、7）.doc）',
};
