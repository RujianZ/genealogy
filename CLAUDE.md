# CLAUDE.md — 张氏宗谱可视化 App

把这份文件放在项目根目录。**开工前先整份读完。**
你没有这个项目的前情，这里就是全部背景。

---

## 〇、第一原则：这是一个数据库

**唯一 id ＋ 外键串联的 schema。不是字符串，不是旧脚本。**

谱里出现过的每一个人，一行，一个 id——男女一样，妻、女、夭折没留名的儿子
都是一等公民。任何地方指到人，指的都是 **pid**，不是名字。

```
P-{册}-{页:04d}-{行}-{列}-L{源行号}      同一行几个人加 .1 .2
```

id 锚在**源行**上，所以解析器改进了、字段变了，id 也不动。

### 判定只发生在写 json 的时候，不发生在画卡片的时候

**题面和答案分两格：**

| | 是什么 | 谁写的 | 例 |
|---|---|---|---|
| 题面 | 谱的原话，一字不改 | 解析器 | `parent_candidates`、名目的 `name`＋`字`、`raw_text` |
| 答案 | 一个 pid | 判定层，配一次写回文件 | `parent_edges`、名目的 `pid` |

写答案的是 `tools/writeback.mjs`（父子边）和 `tools/revlink.mjs`（修谱名目）。
**卡片只读答案。渲染路径里一处拿字符串配人都不许有。**

为什么这条最要紧：同一段配名字的代码，放在写 json 的时候和放在画卡片的时候，
**答案可能一样，但性质完全不同**——

- 写进文件的那次：过了 resolver、过了闸，难的几十个还有人翻回谱面写下依据和核对页码。
  那是一条**有据可查的记录**。
- 渲染时现配的那次：没人看过，没地方写依据，下次刷新可能就变。
  那是一次**没人负责的猜测**。

而且因为它长在页面上，看的人**根本分不出这两者**。
所有「谱上有两个，不知道是哪一个」这类恶心话，一句也不是判定层算出来的
（两个生父 0 人、说不清 0 人），全是渲染时配不准写的搪塞话。**字符串是病根。**

### 解释只许发生一次，就在原文变成数据那道口子上

谱横跨 306 年、十届人修，「生于／殁于／葬」真的就有七种写法——那是历史事实，
没有哪个 schema 能让它消失。**解析器（`parser/fields.py`）的活儿就是认这七种。**

规矩是：**解释只许发生在那一道口子上。过了那道口子，零解释。**

所以「别加一堆规则」指的是**下游**，不是解析层。
解析层认七种写法是规则在对的地方；`entries.ts` 渲染时拿名字配人是规则在错的地方。
别把这条用成「解析器也不许有规则」。

### 「不知道」是一等值，不是 NULL

「生殁缺」是数据。59 个待人工核的是数据。
普通 schema 把这些统统记成 NULL，于是**「我们不知道」和「谱自己写了不知道」就分不开了**
——而那正是这部谱最要紧的区分，凡例写着「阙其所未知」。

所以：**判不出来就不判**（把谱的原话平摆着，不加评语），
**但不判也要留痕**——进待核清单、上疑点页。
不然「阙」就变成了「看不见」。那恰恰是 1992 年那届人在谱上写
「如有错误，谨请先祖赦罪」时踩的坑：他们不是不想留白，
是**没有地方把不确定单独记下来**，只能硬接一条。

### 这套架构的代价，得认

pid 写死之后卡片只读，**一个错的 pid 和一个对的 pid 在页面上长得一模一样**，
全是自信的样子。旧那套至少还会喊「同名有两个」，虽然喊得难听。

所以闸不是可选项，是**架构的价钱**：

```
node tools/check.mjs        九组一起跑
verify_all · smoke · noloss · idcheck · samepid · onlynew · nowaffle · fk · nohardcode
```

`idcheck` 查每一处指向人的引用**以及写在别处的 pid**（revisions.json 曾经整份
241 个 pid 全失效，三道闸都没发现）；`fk` 查生父不超一位；
`nowaffle` 扫三万多段渲染文字里有没有含糊话。
**将来嫌慢想砍的时候，记得砍掉的是这个设计的安全带。**

### 旧的一律删，不做兜底

旧 people.json、旧格式 pid、旧脚本、平行的第二套判定——
它们不是「另一种实现」，**它们就是错的**。两套实现＝两个答案，
而你只会在出事的时候才发现用户看到的是哪一个。

**网站也一样。** 桌面端／手机端／关系计算器，不管做到哪一步，
一律走唯一 id，页面只读不判。

---

## 一、这是什么

湖北黄梅《张氏宗谱》（胜二户），2016 年第十届续修，全四册 1,314 页。
上溯到南宋 1227 年的迁梅始祖张胜二，到今天二十七世，跨 772 年。

解析（`parser/`）、判定（`src/core/resolve.ts`）、闸（`tools/check.mjs`）都已跑通，
产出 `data/people.json` ＋ 全表 5,065 个唯一 id。**剩下的是 app：桌面 + 手机两套布局。**

用户是家族成员，年龄跨度从二十几到八十几。
**做给自己家人用，不是产品，不做商业化。**

---

## 二、三条不可违反的原则

这三条不是风格偏好，是这个项目存在的理由。**任何时候不确定，回来看这里。**

### 1. 不猜

数据里一个人可能有多个候选父亲（真重名）。
**界面必须把候选全部显示成分叉，绝不自动接一条。**

`parent_edges` 是个数组，长度可能大于 1。
不许 `parent_edges[0]`，不许"取最可能的"，不许 `.find()` 取第一个。

### 2. 不漏

搜索结果**不许截断**。匹配到 200 条就显示 200 条。
不许 `.slice(0, 10)`，不许"只显示最相关的"。
低分结果照样列，只是排在后面。

### 3. 可追溯

每一条信息都能一步回到原文。
**每张人物卡底部固定显示出处 + 「查看原文」按钮**，点开是 `raw_text` 原段。

这一条对用户的父亲特别重要——他是律师，看东西的方式是
"这个结论的依据在哪"。这条比任何视觉设计都值钱。

---

## 三、数据契约

`data/people.json`，**2,233 条有独立条目的人**。加上妻／女／无条目的子，
全表 **5,065 个唯一 id**（附记之人由 `kin` 在装载时 materialize，见 `src/core/persons.ts`）。
核心数据不到 1 MB，全部塞进 app——不需要服务器、不需要联网。

```jsonc
{
  "pid": "P-册3-0205-4-1-L8730",   // 唯一主键，自带出处坐标；L 后面是源行号
  "name": "壁火",                    // 归一化后的谱名
  "name_raw": "壁 火",               // 原文（中间有排版空格）
  "gen": 24,                        // 世次

  "zi":  {"text": "火生", "lines": [8732]},   // 字，带原文行号
  "hui": null, "hao": null, "ming": null,     // 讳 / 号 / 名

  "aliases": [                      // 这个人的所有叫法，搜索索引全部覆盖
    {"form": "壁火", "why": "谱名"},
    {"form": "火生", "why": "字"}
  ],

  "father_name": "光量",             // 原文写的父名（字符串，不是 id）
  "filiation": "长子",
  "father_src": "行内",              // 或「页眉指向「子幼均继」」
  "is_heir": false,                 // 是否嗣子（从 filiation 导出，不另算）

  // ═══ 题面：解析器给的候选，谱的原话，一字不改 ═══
  "parent_candidates": [
    { "child": "P-…-L8730", "parent": "P-…-L8711", "parent_name": "光量",
      "kind": "生父",                          // 生父 / 嗣父
      "evidence": "claim_named", "rank": 1,    // 解析层的初判，仅供判定层参考
      "evidence_cn": "父亲的生子列表点名本人",
      "matched_as": "光量 ≈ 光 量（谱名）",
      "child_src": "…", "parent_src": "…" }
  ],

  // ═══ 答案：判定层写回的结论，**每条一个 pid**（tools/writeback.mjs）═══
  "parent_edges": [                 // ★ 数组：生父 ＋ 嗣父可以并存（凡例第十三则双记）
    { "child": "P-…-L8730", "parent": "P-…-L8711", "parent_name": "光量",
      "parent_src": "册3·卷六·朝阳公世系·第205页·第3行",
      "kind": "生父",
      "level": "原话",                          // 见下表
      "why": "本人写「光量长子」，光量的生子名单里也有他——两边都写" }
  ],

  // ═══ 过继：谱的每一句立嗣/出嗣语句，各自落到 pid ═══
  "adoptions": [
    { "原话": "幼子朝纪出祠梦楚",
      "写在谁那一条": "P-…", "写话人": "林公",
      "说的是谁": "P-…", "去处": "P-…", "去处名": "梦楚",
      "怎么定的": "林公那一条写「幼子朝纪出祠梦楚」" }
  ],

  // ═══ 谱记到、但没有独立条目的人：妻 / 女 / 无条目的子 ═══
  //     每一个都自带 id，装载时成为一等公民（persons.ts）
  "kin": [
    { "at": "P-…-L8779",            // 「记到他的那一行」的坐标，恒有值
      "person": "P-…-L8779",        // 这个人的 id（妻/女＝at；子留空，由判定层并到本人条目）
      "role": "妻", "rel_raw": "娶", "ordinal": "",
      "name_raw": "汪氏", "given": "", "surname": "汪",
      "named": true, "died_young": false, "line_seq": 8779 }
  ],

  "birth":  {"text": "光绪二十六年月日时缺", "lines": [...]},  // 原文，永不改
  "death":  {"text": "民国十九年八月", "lines": [...]},
  "burial": {"text": "葬云山下庄屋东边向东南有碑", "lines": [...]},
  "age":    null,

  "titles": [],                     // 庠生/贡生/大学…
  "marks":  [{"tag": "有碑", "text": "..."}],   // 出嗣/迁徙/殉难/节烈/传赞…
  "spouses": [{"pid": "P-…", "rel": "娶", "name_raw": "汪氏", "birth": {...}, ...}],
  "sons_claimed": ["继均"],          // 本人条目里「生子X：…」列的名字
  "daughters_claimed": ["适何", "适桂"],
  "unparsed": [{"seq": 8740, "text": "…", "page": 205, "tagged": false}],
  "page_ptrs": [...],               // 页眉指向（行 1 的人没写父名时用）

  "src": {"vol": "册3", "page": 205, "row": 4, "col": 1,
          "juan": "六", "section": "朝阳公世系"},
  "src_human": "册3·卷六·朝阳公世系·第205页·第4行",
  "raw_text": "壁 火\n光量公长子\n字火生\n生于\n光绪二十六年月日时缺殁于\n民国十九年八月"
}
```

### 判到哪一级（`parent_edges[].level`）——界面上必须显示

判据的次序就是**谱自己的次序**：谱的原话 ＞ 谱的定式（世次·行列·房支）＞ 算术 ＞ 说不清。

| level | 含义 | 人数 |
|---|---|---|
| `原话` | 谱自己写的话判出来的（两头都写、或全谱同名唯一、或过继语句写明） | 2,150 |
| `人工核定` | 我逐案翻回谱面核定并写下依据（`data/人工判定.json`） | 21 |
| `定式` | 第一级判不出，靠谱自己的版面规矩（正上一格／同房／夹在兄弟中间）定的 | 40 |
| `谱未写` | 谱上连父名都没有 | 3 |
| `说不清` | 判定层没能定案 | **0** |

**「定式」和「版面/房支对不上」的要在界面上能一眼看出来**——那不是谱的原话。
这两类合计 59 人，全部列在**疑点页**和 `work/待核清单.md`，等人回谱面核。

`parent_candidates` 里的 `rank`／`evidence` 是**解析层的初判**，
只做判定层的输入，**界面不要读它**——要读就读 `parent_edges[].level` 和 `.why`。

---

## 四、必须知道的领域知识

不懂这几条，你会写出错的 UI。

### 过继：一个人可能有两个父亲

第 17 世**启昌（字焕先）**是关键案例：

- **生父** 朝相公（梦庚公支）
- **嗣父** 朝阳公（梦林公支）

族谱凡例明文要求双记，理由是「**不忘所自出**」。
所以数据里他有两条父边，`kind` 分别是「生父」和「嗣父」。

**界面必须能切换"血缘线"和"宗法线"**，两条链在第 17 世分叉、
在第 9 世世昂公合回去。这不是 bug，是这个家族真实的结构。
全谱有 143 个嗣子/祧子。

### 世次是硬约束

父亲必须正好高一世（`gen` 差 1）。世次由原书自己的
「第一世…第五世」世代列头标死，不是推算出来的。

上溯时可以用它把路径分成「世次单调」和「不单调」两组，
**但两组都要显示**，不许把不单调的删掉。

### 女性没有独立条目

妻子作为 `spouses` 附在男性下，只记姓（「妣汪氏」），
第 25 世之后才开始有名字（「娶李氏雪梅」）。
女儿只有 `daughters_claimed: ["适陈"]`，只有夫家姓。

**数据模型必须容纳这种不对称。不要假设每个人都有独立节点。**

### 空值是有含义的

`birth.text` 可能是「光绪二十六年**月日时缺**」——
"缺"是编谱人明确写下的"这里没有记录"，不是数据缺失。
**界面上要原样显示，不要显示成空白或"未知"。**

### 别做年号转公历

原文「民国十九年八月」，实际是 1931 年 1 月 25 日——
按农历在庚午年**腊月**，庚午年正是民国十九年。**年份对，月份错。**

这类转换每一次都是判断。第一段有意没做。
**你也不要做。** 界面只显示原文。

---

## 五、技术选型（已定，别推翻）

| 层 | 选择 | 理由 |
|---|---|---|
| 数据 | **静态 JSON 打进包** | 不到 1MB，离线可用，无服务器 |
| 前端 | **TypeScript + React** | 类型安全；类型从 pydantic 导 JSON Schema 生成 |
| 桌面 | **Tauri** | Rust 壳几 MB。**不要 Electron**，100MB+ 起步 |
| 手机 | **PWA** | 加到主屏就是图标。**最好的安装方式是不用安装** |
| 搜索 | **本地确定性算法** | 2,258 人暴力比对是毫秒级 |

### 明确不要用向量 / embedding

人名两三个字**没有语义可嵌入**。"张承健"和"张呈祥"的向量距离说明不了任何东西。
而且向量只给分数、说不出理由，违反"可追溯"原则。

要的是：字形归一 + 同音折叠 + 编辑距离 + 字段加权，**每条结果都能说清为什么命中**。
参考 `gpx2/search.py`，逻辑直接翻译成 TS 即可。

---

## 六、界面要求

### 桌面端 = 研究工具（高密度）

```
┌ 搜索框（名/字/讳/号，模糊，结果不截断）
├ 左栏：直系上溯链（27→1，一屏装得下）
├ 中栏：当前人物全字段 + 出处 + [查看原文]
└ 右栏：同辈 / 子女 / 配偶 / 反向引用（谁的原文里提到过他）
```

每行可点，点了换焦点。整个 app 就是一次深度优先遍历。

### 手机端 = 查询工具（给长辈，另一套布局，不是缩小版）

**长辈打不了字。** 繁体、生僻字（銑、硺、澐、彛）、拼音输入法，每样都是墙。

**① 第一次打开：分级选择"我是谁"，不打字**
```
选房（学义房/学仁房/学信房/朝寿房…）→ 选辈（继/开/承/宏）→ 点人
```
选完记住，以后每次打开就是"你"。

**② 主界面是一条从自己往上的竖链**，上下滑，点任意一环换焦点。
手势只有两个：滑、点。

**③ 搜索是备用，不是主功能。**

**④ 关系计算器**——选两个人，算出该叫什么。
纯算法，绝对可靠，过年时全家会围着看。

### 视觉

- **加载页**用族谱那个红底龙凤封面，淡出。**只出现一次，别做常驻背景**
- **取色**从封面图采样正红，配宣纸米色 + 墨黑。别自己调"中国红"，会调成塑料感
- **字体**思源宋体 / Noto Serif SC。**族谱就该是宋体**，黑体像政府网站
- **别竖排**——手机上竖排中文可读性极差。竖向的是世系链的方向，不是文字方向
- **别做全族树状图**——2,258 人画出来是一团糊
- **别加水墨动效、卷轴展开、毛笔笔触**。一次就够，多了变廉价古风

用户明确讨厌圆角、大留白、字少的通用 AI dashboard 风格。
桌面端尽管高密度；**手机端给长辈的那版要反过来：字大、行疏、一屏少信息。**
同一份数据两套布局，这是响应式该干的事，不是妥协。

---

## 七、绝对不要做的事

- ❌ **在渲染路径里拿名字配人**——判定只在写 json 的时候做（见第〇节）
- ❌ **写「谱上有两个，不知道是哪一个」这类话**——判不出来就不判，什么都别写
- ❌ 界面读 `parent_candidates` 里的 `rank`／`evidence`（那是解析层的初判，读 `level`／`why`）
- ❌ 留一份平行的第二套判定、或任何形式的兜底
- ❌ `parent_edges[0]` 或任何形式的"取第一个候选"
- ❌ 搜索结果 `.slice()` 截断
- ❌ 年号 / 农历转换
- ❌ 改写、清洗、"修正"任何原文字段
- ❌ 把 `birth.text` 里的"缺""未详"渲染成空白
- ❌ Electron
- ❌ 向量检索
- ❌ 后端服务器、数据库、登录、账号
- ❌ 任何形式的商业化、分析埋点、广告

---

## 八、现在到哪一步了

第八节原来写的是「先把这三件事跑通」（搜「火生」、承健 27 代上溯、点开看原文）。
**三件都跑通了，而且早已长成一套完整的判定 + 闸。** 现在的账：

```
唯一 id            5,065（有条目 2,233 ＋ 附记之人 2,832：妻 · 女 · 无条目的子）
父子关系可断定      2,171 人 = 97.2%    需人工回谱面核 59 人    说不清 0
两个生父            0 人
未归属原文          1,215 行            档案层蒸发 0 字
界面含糊话          0 处（扫了 36,672 段用户能看见的文字）
修谱名目            191 人 → 定到 pid 159，平摆着 32，死链 0
九组闸              全过（node tools/check.mjs）
打包                build/张氏宗谱.html  17.5 MB 离线单文件
```

### 跑一遍全链

```bash
python -m parser.build parser/jsonl build/新              # 解析
cp build/新/people.json data/people.json
python tools/build_prose.py && python tools/classify_prose.py
cp data/prose.json data/prose_ents.json
python tools/extract_entities.py && python tools/attribute_prose.py
python tools/build_burials.py && python tools/build_places.py
node tools/writeback.mjs        # ★ 父子边：题面 → 答案（写 pid）
node tools/revlink.mjs          # ★ 修谱名目：名字 → 答案（写 pid）
node tools/check.mjs            # ★ 九组闸，必须全过
node tools/bundle.mjs           # 打成单文件 HTML
```

### 还欠的（按「先改数据、后改界面」排）

**数据层——这些是结构问题，要开字段，不是加规则**

1. 附记之人（女儿、无条目的儿子）**没有自己的生卒格子**。谱写在父亲条目里的
   会落到妻子头上（宏震@册4「生女一　名美琳生于二0一0年三月十日」记到了王慧萍名下）。
2. 功名／学历／职业（「初中生」「郡文学生」「武汉大学」95 行）没有字段。
3. 法名（「法名履贞」）没有字段。
4. 兄弟姐妹栏里 289 条其实是他自己的另一条记载（兼祧印了几条）——
   搜索那一层早就 `canonical` 折回了，兄弟栏没走同一套。

**⚠ 有一处试过、失败了、别再试**

葬地归属的位置推断（`parser/fields.py` 的 `_whose` → `_own_filled`）：
夫妻同一段里，第一条无前缀的「葬…」给丈夫。绝大多数对（俱葬／合葬确实是两人），
妻子另有葬地、丈夫的葬写在后面时会错——光辉@册2 p318、梁英@册3 p110。
**改成「在配偶段里就归她」试过：442 处变动，胜二公自己的坟都没了。别改。**

**人工待核（都可重跑，都有清单）**

| 文件 | 是什么 | 条数 |
|---|---|---|
| `work/待核清单.md` | 父子关系要人回谱面核的 | 59 |
| `work/名目待核.json` | 修谱名目定不到人的 | 32 |
| `work/谱面勘误.md` | 谱自己印错的地方 | — |
| `work/人工核对记录.md` | 逐案核定的依据和核对页码 | — |
| `work/台账.json` | 每个人判到哪一级、交叉验证过没有 | 2,233 |

**app（还没开始）**

桌面端三栏高密度研究工具 ｜ 手机端给长辈另一套布局 ｜ 关系计算器 ｜ Tauri ＋ PWA。
**不管做到哪一步，一律走第〇节：所有指向人的地方都是 pid，页面只读不判。**

---

## 九、附：项目背景（不影响技术决策，但有助于理解为什么这么较真）

这部谱从 1710 年首修到 2016 年，十届，306 年。
历代序里反复出现同一句编纂准则：

> **不缘情而增，不故意而减**
> **纪其所可知，阙其所未知**

1992 年那届重修时，全族手上只剩四个人藏的十二本残卷，
中间断了 118 年。有人在谱上写下这么一句：

> 因社会变更，旧谱难以保存，间断一百廿余年，后世无法记忆先祖字号，
> 故系壁松公为祖父。**如有错误，谨请先祖赦罪。**

**这个项目做的是同一件事的第十一环。**
不猜、不漏、可追溯——不是技术洁癖，是接着他们的规矩往下做。
