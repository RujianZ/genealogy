# CLAUDE.md — 张氏宗谱可视化 App

把这份文件放在项目根目录。**开工前先整份读完。**
你没有这个项目的前情，这里就是全部背景。

---

## 一、这是什么

湖北黄梅《张氏宗谱》（胜二户），2016 年第十届续修，全四册 1,314 页。
上溯到南宋 1227 年的迁梅始祖张胜二，到今天二十七世，跨 772 年。

第一段（Python 解析 + 校验）**已经做完并跑通**，产出 `people.json`。
**你的任务只有第二段：一个桌面 + 手机端的查询 app。**

用户是家族成员，年龄跨度从二十几到八十几。
**做给自己家人用，不是产品，不做商业化。**

---

## 二、三条不可违反的原则

这三条不是风格偏好，是这个项目存在的理由。**任何时候不确定，回来看这里。**

### 1. 不猜

数据里一个人可能有多个候选父亲（真重名）。
**界面必须把候选全部显示成分叉，绝不自动接一条。**

`parent_edges` 是个数组，长度可能大于 1。
不许 `parent_edges[0]`，不许"取最可能的"，不许 `.find()` 取第一个。

### 2. 不漏

搜索结果**不许截断**。匹配到 200 条就显示 200 条。
不许 `.slice(0, 10)`，不许"只显示最相关的"。
低分结果照样列，只是排在后面。

### 3. 可追溯

每一条信息都能一步回到原文。
**每张人物卡底部固定显示出处 + 「查看原文」按钮**，点开是 `raw_text` 原段。

这一条对用户的父亲特别重要——他是律师，看东西的方式是
"这个结论的依据在哪"。这条比任何视觉设计都值钱。

---

## 三、数据契约

`people.json`，约 2,258 条，gzip 后约 400KB。
**核心数据不到 1MB，全部塞进 app，不需要服务器、不需要数据库、不需要联网。**

```jsonc
{
  "pid": "P-册3-0205-4-1-0",        // 唯一主键，自带出处坐标
  "name": "壁火",                    // 归一化后的谱名
  "name_raw": "壁 火",               // 原文（中间有排版空格）
  "gen": 24,                        // 世次

  "zi":  {"text": "火生", "lines": [1234]},   // 字，带原文行号
  "hui": null,                                 // 讳
  "hao": null,                                 // 号
  "ming": null,                                // 名

  "aliases": [                      // 这个人的所有叫法，搜索索引全部覆盖
    {"form": "壁火", "why": "谱名"},
    {"form": "火生", "why": "字"}
  ],

  "father_name": "光量",             // 原文写的父名（字符串，不是 id）
  "filiation": "长子",
  "father_src": "行内",              // 或「页眉指向「子幼均继」」
  "is_heir": false,                 // 是否嗣子（过继而来）

  "parent_edges": [                 // ★ 数组！可能多条！
    {
      "parent": "P-册3-0205-3-1-0",
      "parent_name": "光量",
      "kind": "生父",                // 生父 / 嗣父
      "evidence": "claim_named",
      "rank": 1,                    // 1 最硬，5 最弱
      "evidence_cn": "父亲的生子列表点名本人",
      "matched_as": "光量 ≈ 光 量（谱名）",
      "parent_src": "册3·卷六·朝阳公世系·第205页·第3行"
    }
  ],

  "birth":  {"text": "光绪二十六年月日时缺", "lines": [...]},  // 原文，永不改
  "death":  {"text": "民国十九年八月", "lines": [...]},
  "burial": {"text": "葬云山下庄屋东边向东南有碑", "lines": [...]},
  "age":    null,

  "titles": [],                     // 庠生/贡生/大学…
  "marks":  [{"tag": "有碑", "text": "..."}],   // 出嗣/迁徙/殉难/节烈/传赞…
  "spouses": [{"rel": "娶", "name_raw": "汪氏", "birth": {...}, ...}],
  "sons_claimed": ["继均"],          // 本人条目里「生子X：…」列的名字
  "daughters_claimed": ["适何", "适桂"],
  "unparsed": [{"seq": 1240, "text": "…", "page": 205, "tagged": false}],

  "src": {"vol": "册3", "page": 205, "row": 4, "col": 1,
          "juan": "六", "section": "朝阳公世系"},
  "src_human": "册3·卷六·朝阳公世系·第205页·第4行",
  "raw_text": "壁 火\n光量公长子\n字火生\n生于\n光绪二十六年月日时缺殁于\n民国十九年八月"
}
```

### 依据等级（`rank`）——界面上必须显示

| rank | evidence | 含义 |
|---|---|---|
| 1 | `claim_named` | 父亲的生子列表点名了本人（最硬） |
| 2 | `sole_homonym` | 全谱同名只有一个 |
| 3 | `stated_adopt` | 过继语句原文写明 |
| 4 | `honorific` | 去掉敬称「公」后同名 |
| 5 | `homonym_one_of` | 多个同名候选之一（**最弱，必须显眼**） |

**rank 5 的边在界面上要能一眼看出来。** 那是"谱上只写了两个字，没说是哪个"。

---

## 四、必须知道的领域知识

不懂这几条，你会写出错的 UI。

### 过继：一个人可能有两个父亲

第 17 世**启昌（字焕先）**是关键案例：

- **生父** 朝相公（梦庚公支）
- **嗣父** 朝阳公（梦林公支）

族谱凡例明文要求双记，理由是「**不忘所自出**」。
所以数据里他有两条父边，`kind` 分别是「生父」和「嗣父」。

**界面必须能切换"血缘线"和"宗法线"**，两条链在第 17 世分叉、
在第 9 世世昂公合回去。这不是 bug，是这个家族真实的结构。
全谱有 143 个嗣子/祧子。

### 世次是硬约束

父亲必须正好高一世（`gen` 差 1）。世次由原书自己的
「第一世…第五世」世代列头标死，不是推算出来的。

上溯时可以用它把路径分成「世次单调」和「不单调」两组，
**但两组都要显示**，不许把不单调的删掉。

### 女性没有独立条目

妻子作为 `spouses` 附在男性下，只记姓（「妣汪氏」），
第 25 世之后才开始有名字（「娶李氏雪梅」）。
女儿只有 `daughters_claimed: ["适陈"]`，只有夫家姓。

**数据模型必须容纳这种不对称。不要假设每个人都有独立节点。**

### 空值是有含义的

`birth.text` 可能是「光绪二十六年**月日时缺**」——
"缺"是编谱人明确写下的"这里没有记录"，不是数据缺失。
**界面上要原样显示，不要显示成空白或"未知"。**

### 别做年号转公历

原文「民国十九年八月」，实际是 1931 年 1 月 25 日——
按农历在庚午年**腊月**，庚午年正是民国十九年。**年份对，月份错。**

这类转换每一次都是判断。第一段有意没做。
**你也不要做。** 界面只显示原文。

---

## 五、技术选型（已定，别推翻）

| 层 | 选择 | 理由 |
|---|---|---|
| 数据 | **静态 JSON 打进包** | 不到 1MB，离线可用，无服务器 |
| 前端 | **TypeScript + React** | 类型安全；类型从 pydantic 导 JSON Schema 生成 |
| 桌面 | **Tauri** | Rust 壳几 MB。**不要 Electron**，100MB+ 起步 |
| 手机 | **PWA** | 加到主屏就是图标。**最好的安装方式是不用安装** |
| 搜索 | **本地确定性算法** | 2,258 人暴力比对是毫秒级 |

### 明确不要用向量 / embedding

人名两三个字**没有语义可嵌入**。"张承健"和"张呈祥"的向量距离说明不了任何东西。
而且向量只给分数、说不出理由，违反"可追溯"原则。

要的是：字形归一 + 同音折叠 + 编辑距离 + 字段加权，**每条结果都能说清为什么命中**。
参考 `gpx2/search.py`，逻辑直接翻译成 TS 即可。

---

## 六、界面要求

### 桌面端 = 研究工具（高密度）

```
┌ 搜索框（名/字/讳/号，模糊，结果不截断）
├ 左栏：直系上溯链（27→1，一屏装得下）
├ 中栏：当前人物全字段 + 出处 + [查看原文]
└ 右栏：同辈 / 子女 / 配偶 / 反向引用（谁的原文里提到过他）
```

每行可点，点了换焦点。整个 app 就是一次深度优先遍历。

### 手机端 = 查询工具（给长辈，另一套布局，不是缩小版）

**长辈打不了字。** 繁体、生僻字（銑、硺、澐、彛）、拼音输入法，每样都是墙。

**① 第一次打开：分级选择"我是谁"，不打字**
```
选房（学义房/学仁房/学信房/朝寿房…）→ 选辈（继/开/承/宏）→ 点人
```
选完记住，以后每次打开就是"你"。

**② 主界面是一条从自己往上的竖链**，上下滑，点任意一环换焦点。
手势只有两个：滑、点。

**③ 搜索是备用，不是主功能。**

**④ 关系计算器**——选两个人，算出该叫什么。
纯算法，绝对可靠，过年时全家会围着看。

### 视觉

- **加载页**用族谱那个红底龙凤封面，淡出。**只出现一次，别做常驻背景**
- **取色**从封面图采样正红，配宣纸米色 + 墨黑。别自己调"中国红"，会调成塑料感
- **字体**思源宋体 / Noto Serif SC。**族谱就该是宋体**，黑体像政府网站
- **别竖排**——手机上竖排中文可读性极差。竖向的是世系链的方向，不是文字方向
- **别做全族树状图**——2,258 人画出来是一团糊
- **别加水墨动效、卷轴展开、毛笔笔触**。一次就够，多了变廉价古风

用户明确讨厌圆角、大留白、字少的通用 AI dashboard 风格。
桌面端尽管高密度；**手机端给长辈的那版要反过来：字大、行疏、一屏少信息。**
同一份数据两套布局，这是响应式该干的事，不是妥协。

---

## 七、绝对不要做的事

- ❌ `parent_edges[0]` 或任何形式的"取第一个候选"
- ❌ 搜索结果 `.slice()` 截断
- ❌ 年号 / 农历转换
- ❌ 改写、清洗、"修正"任何原文字段
- ❌ 把 `birth.text` 里的"缺""未详"渲染成空白
- ❌ Electron
- ❌ 向量检索
- ❌ 后端服务器、数据库、登录、账号
- ❌ 任何形式的商业化、分析埋点、广告

---

## 八、第一步做什么

**不要先搭框架。先把数据吃进去，用最丑的 HTML 把这三件事跑通：**

1. 搜「火生」，把全部命中列出来（应该有 3 个字火生的 + 5 个差一字的）
2. 输入"承健"，把 27 代上溯链列出来，**在第 17 世显示出生父/嗣父两条分叉**
3. 随便点一个人，底部显示 `src_human` 和 `raw_text`

**这三件跑通了，剩下的都是样式。** 反过来先做样式，数据模型一定返工。

---

## 九、附：项目背景（不影响技术决策，但有助于理解为什么这么较真）

这部谱从 1710 年首修到 2016 年，十届，306 年。
历代序里反复出现同一句编纂准则：

> **不缘情而增，不故意而减**
> **纪其所可知，阙其所未知**

1992 年那届重修时，全族手上只剩四个人藏的十二本残卷，
中间断了 118 年。有人在谱上写下这么一句：

> 因社会变更，旧谱难以保存，间断一百廿余年，后世无法记忆先祖字号，
> 故系壁松公为祖父。**如有错误，谨请先祖赦罪。**

**这个项目做的是同一件事的第十一环。**
不猜、不漏、可追溯——不是技术洁癖，是接着他们的规矩往下做。
